-- AlippHUB | Auto Walk VIP - FINAL FIX WITH TAB HIDE/SHOW
local WindUI = loadstring(game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua"))()

local CONFIG = {
    HubName = "AlippHUB",
    Subtitle = "Auto Walk",
    Discord = "dsc.gg/alipphub",
    ValidKeys = {"TESTKEY123", "DEMO2024", "RULLZSY2024", "alippvip123"} -- Ganti key kamu
}

local player = game.Players.LocalPlayer
local _G = { Verified = false }

local function notify(title, content, duration)
    WindUI:Notify({Title = title, Content = content, Duration = duration or 5})
end

-- Window instant no loading
local Window = WindUI:CreateWindow({
    Title = CONFIG.HubName .. " | Authentication",
    SubTitle = CONFIG.Subtitle,
    Theme = "Dark",  -- Gelap mirip screenshot asli
    Acrylic = false  -- No stuck loading!
})

-- Buat semua tab dulu
local AuthTab = Window:Tab({Title = "Authentication", Icon = "lock"})

local AutoTab = Window:Tab({Title = "Auto Walk", Icon = "walk"})
local CredTab = Window:Tab({Title = "Credits", Icon = "heart"})
local BypassTab = Window:Tab({Title = "Bypass", Icon = "shield"})
local HideTab = Window:Tab({Title = "Hide Menu", Icon = "eye-off"})
local AccountTab = Window:Tab({Title = "Account", Icon = "user"})
local CopyTab = Window:Tab({Title = "Copy Avatar", Icon = "copy"})
local AnimTab = Window:Tab({Title = "Animation", Icon = "sparkles"})
local SkyTab = Window:Tab({Title = "Skybox", Icon = "cloud"})
local PlayerTab = Window:Tab({Title = "Player Menu", Icon = "users"})
local SocialTab = Window:Tab({Title = "Social Media", Icon = "share"})
local SettingsTab = Window:Tab({Title = "Settings", Icon = "cog"})

-- Hide semua tab kecuali Auth
AutoTab:Hide()
CredTab:Hide()
BypassTab:Hide()
HideTab:Hide()
AccountTab:Hide()
CopyTab:Hide()
AnimTab:Hide()
SkyTab:Hide()
PlayerTab:Hide()
SocialTab:Hide()
SettingsTab:Hide()

-- Isi content tab (contoh, tambahin full dari file kamu)
-- Auto Walk content
local fpsSec = AutoTab:Section({Title = "FPS Booster"})
fpsSec:Toggle({Title = "Activate FPS Booster", Default = false, Callback = function(state)
    settings().Rendering.QualityLevel = state and 1 or 10
end})

local nameSec = AutoTab:Section({Title = "Nametag Menu"})
nameSec:Toggle({Title = "Hide Nametag", Default = false})

local walkSec = AutoTab:Section({Title = "Walk Speed Menu"})
walkSec:Toggle({Title = "Enable Walk Speed", Default = false})
walkSec:Slider({Title = "Set Walk Speed", Min = 16, Max = 300, Default = 16})

-- Credits content
CredTab:Section({Title = "Special Thanks"}):Paragraph({Title = "", Content = "UI: WindUI\nDev: Alipp\nCommunity: AlippHUB"})

-- Bypass content
BypassTab:Section({Title = "Bypass Features"}):Toggle({Title = "Bypass Admin Detection", Default = false})
BypassTab:Section({Title = ""}):Toggle({Title = "Bypass AFK", Default = false})
BypassTab:Section({Title = ""}):Button({Title = "AUTO CLICKER | PC"})
BypassTab:Section({Title = ""}):Button({Title = "AUTO CLICKER | ANDROID"})

-- Hide Menu content
HideTab:Section({Title = "Themes UI"}):Dropdown({Title = "Preset Themes", Options = {"Dark", "Light", "Rose", "Blue"}, Default = "Dark", Callback = function(choice)
    WindUI:ChangeTheme(choice)
end})
HideTab:Section({Title = "Toggle Menu"}):Keybind({Title = "Toggle UI", Default = Enum.KeyCode.P, Callback = function() Window:Minimize() end})

-- Authentication key system
local keySec = AuthTab:Section({Title = "Key System"})

local statusPara = keySec:Paragraph({Title = "Status", Content = "⚠️ Not Verified - Masukkan key untuk unlock"})

keySec:Paragraph({Title = "Info", Content = "Beli key premium di Discord:\n" .. CONFIG.Discord})

local keyInputValue = ""
keySec:Input({Title = "Input Key", Placeholder = "Masukkan key...", Callback = function(text) keyInputValue = text end})

keySec:Button({Title = "Verify Key", Callback = function()
    if keyInputValue == "" then notify("Error", "Masukkan key dulu!", 4) return end
    if table.find(CONFIG.ValidKeys, keyInputValue) then
        _G.Verified = true
        notify("Success", "✅ Key valid! Semua tab & fitur terbuka!", 6)
        Window:SetTitle(CONFIG.HubName .. " | Auto Walk")
        statusPara:Set({Title = "🌟 VIP VERIFIED 🌟", Content = "Selamat datang " .. player.Name .. "!\nSemua fitur premium aktif."})

        -- SHOW SEMUA TAB!
        AutoTab:Show()
        CredTab:Show()
        BypassTab:Show()
        HideTab:Show()
        AccountTab:Show()
        CopyTab:Show()
        AnimTab:Show()
        SkyTab:Show()
        PlayerTab:Show()
        SocialTab:Show()
        SettingsTab:Show()
    else
        notify("Error", "❌ Key salah!", 5)
    end
end})

notify("AlippHUB", "UI loaded instant! Masukkan key di Authentication.", 7)
